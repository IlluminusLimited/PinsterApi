
# == Schema Information
#
# Table name: authentications
#
#  id               :string(36)       not null, primary key
#  user_id          :string(36)       not null
#  uid              :string(255)      not null
#  provider         :string(255)      not null
#  token            :string(255)      default("")
#  token_expires_at :datetime
#  created_at       :datetime         not null
#  updated_at       :datetime         not null
#
# Indexes
#
#  index_authentications_on_provider_and_uid  (provider,uid)
#  index_authentications_on_token             (token)
#  unique_token                               (token) UNIQUE
#

sally_token:
  user: sally
  uid: sample-auth-response
  provider: sample
  token: sally-auth-token
  token_expires_at: <%= Time.now.utc %>


tom_token:
  user: tom
  uid: tom-auth-response
  provider: sample
  token: tom-auth-token
  token_expires_at: <%= Time.now.utc + 3.hours %>

bob_token:
  user: bob
  uid: bob-auth-response
  provider: sample
  token: bob-auth-token
  token_expires_at: <%= Time.now.utc + 3.hours %>
